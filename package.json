{
  "name": "m74gest",
  "version": "1.0.0",
  "description": "Sistema de gestão M74 - Backend + Frontend",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix vite-project",
    "build": "npm run build --prefix vite-project",
    "start": "npm run start --prefix backend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix vite-project",
    "deploy": "npm run install:all && npm run build && npm run start"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
